import { mainnet } from './mainnet';
import { testnet } from './testnet';
import { regtest } from './regtest';
import { Networks } from '../../types/base';

export const bch: Networks = {
  mainnet,
  testnet,
  regtest,
};
